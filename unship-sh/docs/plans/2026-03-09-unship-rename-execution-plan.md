# Unship Rename Execution Plan

- Rename workspace crates, bundled skill docs, design-language doc, tracked workspace folder, and historical plan filenames from `unship` to `unship`.
- Replace remaining checked-in identifiers and content references across Rust, desktop, docs, workflows, knowledgebase, and tracked task corpus.
- Regenerate derived artifacts and verify with workspace tests, lint, desktop checks, and a final grep enforcement pass.
